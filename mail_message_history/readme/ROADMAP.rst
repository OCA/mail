The document name is filled when a message is created, so the messages logged
before the module was installed keep an empty *Document* column. Filling them
requires a one-off script resolving the name of every documents they are
attached to.
